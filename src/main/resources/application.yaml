spring:
  messages:
    basename: messages
    encoding: UTF-8
  h2:
    console:
      path: /tpo_11-console
      enabled: true
  datasource:
    url: jdbc:h2:mem:linkDB
    username: sa
    password:
  application:
    name: TPO_11
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
    defer-datasource-initialization: true
